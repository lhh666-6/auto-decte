# Web 三角色端与后台服务转型最终验收

验收日期：2026-07-23

## 交付范围

| 阶段 | 结果 | 核心交付 |
|---|---|---|
| 0 | 通过 | 移动端契约冻结、边界脚本和 CI 门禁 |
| 1 | 通过 | 管理员、财务、厂长统一 Web 登录与权限隔离 |
| 2 | 通过 | 电子表单版本、管理员审批、按厂启用和通知 |
| 3 | 通过 | 财务业务梳理、确认基线、流程图、预检、审批和启用 |
| 4 | 通过 | 原子提交、不可变账本、北京时间投影、更正链和待办 |
| 5 | 通过 | 工资规则审批、版本绑定、历史重算、二次确认和访问审计 |
| 6 | 通过 | XLS/XLSX 安全解析、映射版本、正式导出、水位和单元格血缘 |
| 7 | 通过 | 旧桌面识别入口退役、历史表归档、备份清理工具 |

## 最终门禁

- 后端新主线与冻结移动端组合验收：`111 passed`
- 前端全量 Vitest：`291 passed`
- Ruff：通过
- Mypy strict：通过
- Web TypeScript typecheck：通过
- 移动端边界：基线至当前提交无冻结目录改动
- Alembic：空库可升级到 `027`
- Alembic `027` 后生产模式服务装配：通过
- 真实旧版 XLS：`叉车工日工资计算表.xls` 成功识别 1 个工作表

## 运行边界

- 新管理端入口：`/login`
- 移动端入口：`/mobile/login`
- 管理端工作区：`/admin/*`、`/finance/*`、`/plant/*`
- 旧桌面图片识别入口不再提供。
- 历史识别数据只读归档，不纳入新主线写入。

## 上线动作

上线窗口停止旧服务后：

1. 备份当前 `data`；
2. 执行 Alembic 升级到 `027`；
3. 先以默认 dry-run 运行旧证据清理工具并复核报告；
4. 确认备份 ZIP 可读取后，再使用 `--apply`；
5. 启动 API 与 Web，验证 `/health/live`、三角色登录和移动端登录。

无 Alembic 版本号的历史本地库已覆盖自动接管测试，接管后统一升级至 `027`，旧识别表进入 `legacy_archive_*` 只读归档。
