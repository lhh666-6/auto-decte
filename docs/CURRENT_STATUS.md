# 当前项目状态

最后更新：2026-07-19

## V2 开发主线

用户已确认以 [工业纸质工资表系统：低 Token 实施与统一 UI 规范](superpowers/specs/2026-07-19-industrial-payroll-low-token-ui-v2.md) 为新的最终产品要求。该规范把现有 10 张车间模板进一步收敛为 6 个核心纸质版面与可版本化岗位配置，并新增正式打印/识别调试分层、批量图片、图片总览、多报表和只读 AI。此前 Task 1—16 的通过结果继续作为基线，不重复验收；它们不能替代 V2 新增要求。

第一个差异任务 [模板预览正式打印层与识别调试层分离](superpowers/plans/2026-07-19-template-preview-layer-separation.md) 已完成：网页默认显示真实 PNG 打印产物，主动选择“显示识别区域”后才叠加字段框。局部验证为组件测试 `4 passed`、前端 TypeScript 类型检查通过；浏览器以真实“计时工日工资表”确认默认 1 张业务图片、0 个识别框，切换后仍为 1 张图片并出现 22 个识别框。未重复运行既有 Python 全套测试。

下一差异任务是 [6 个核心版面与岗位配置的领域模型](superpowers/plans/2026-07-19-core-layout-job-profile-domain.md)。只建立领域边界，不迁移数据、不改 API、不重写六张版面。

该领域任务已完成：V2 只有六个 `CoreLayoutKind`，岗位配置版本明确绑定一个模板版本，并具备草稿、就绪、发布、停用和从已发布版本复制新草稿的生命周期。验证为 `18 passed`，Ruff 和 mypy 通过。下一任务是 [岗位配置持久化](superpowers/plans/2026-07-20-job-profile-persistence.md)。

岗位配置持久化已完成：新增 `job_profile_versions` 和纯增量 Alembic 009，仓储可按 ID 或“岗位键 + 版本”读取，并保护已发布配置内容。验证为仓储 `7 passed`、迁移完整性 `18 passed`、Ruff 和 mypy 通过；迁移测试证明从 008 升级后旧模板仍可读取。下一任务是 [岗位配置应用服务](superpowers/plans/2026-07-20-job-profile-application-service.md)。

岗位配置应用服务已完成：HTTP 之外已有创建、读取、列表、修改、发布、复制和停用用例；发布时强制验证绑定模板版本存在且版本号一致。验证为 `3 passed`、Ruff 和 mypy 通过。下一任务是 [岗位配置 HTTP API](superpowers/plans/2026-07-20-job-profile-api.md)。

岗位配置 HTTP API 已完成：本地前端可创建、列表、读取、修改、发布、复制和停用岗位配置，路由只调用应用服务。模板 API 全文件验证 `13 passed`，Ruff 和 mypy 通过。下一任务是 [模板 + 岗位配置双版本二维码](superpowers/plans/2026-07-20-dual-version-qr.md)。

## 本轮结论

工业表单前端重建实施计划的任务 1—16 已按顺序完成。协作同步基线为 `ff03a39 fix(review): preserve confirmed values across reload and export`，并已在其上完成新主线 Task 1—10；集成分支为 `modular-architecture`。

**最终开发计划的代码与自动化已完成，Task 16 的实体闭环尚未完成。** [工业纸质工资表闭环与前端交互重构最终计划](superpowers/plans/2026-07-18-industrial-paper-templates-implementation.md)已交付本地无登录完整权限、前端信息架构、字段行为模型、3 类母版、10 个车间模板、任意毫米纸张、拼版、二维码、工号核验、姓名人工确认、Excel 导出和三个代表模板的数字模拟纸面闭环。计划完成判定仍要求三类母版实体闭环；本环境没有实体打印机、手机和卡尺，所以尚不能标记整个计划完全完成。

本轮没有改变审核事务、模板版本、主数据乐观锁、导出批次或识别算法等后端业务不变量。前端已从单页功能区切换重建为有固定路由、统一外壳和中文业务语言的 React Web 应用。

## 新主线执行进度

### Task 1：本地无登录完整权限与中文错误边界（已完成）

- 默认 `APP_AUTH_MODE=local_full_access`；本地操作员保留 `OPERATOR` 角色和 `local-operator` 审计主体，但可使用全部业务权限，不再因模板、审核、导入、基础数据、导出或重导权限形成死路。
- 权限矩阵没有删除；切换到 `APP_AUTH_MODE=authenticated` 后，显式身份继续按角色权限受限。
- 完整权限只改变授权判断，不绕过原有服务和事务；基础数据写入复测确认仍生成 `CREATE` 审计事件，操作人为 `local-operator`。
- 统一页头已删除“当前身份”和用户菜单占位，只显示“本地单机模式 · 服务正常”。
- 模板、审核、基础数据和导出页面使用统一中文错误边界，不再把 `PERMISSION_DENIED`、`template.read` 或 `Actor ... lacks ...` 等内部信息展示给工作人员。
- 后端通用错误标题和异常提示已中文化，权限拒绝响应不再包含具体内部权限名。

Task 1 定向验证：Python `13 passed`；前端 `3` 个测试文件、`8 passed`；TypeScript、相关 Ruff 和 mypy 通过。真实浏览器在模板中心确认页头无伪身份入口、无内部权限码。完整回归仍按计划只在 Task 16 运行。

### Task 2：全局导航与审核工作台信息架构（已完成）

- 产品品牌统一为不可点击的“工业工资表系统”；全局顶部只保留审核工作台、模板中心、基础数据、导出数据四个一级入口。
- 删除审核页内部重复页头、左侧模块入口及“可导出”队列；待确认表单类型、待核对、待重新拍照改为工作台内部标签。
- 模板、基础数据和导出一级页面不再显示“返回审核工作台”，模块切换统一使用顶部导航和浏览器历史。
- 三个审核队列保持独立 URL；浏览器实测从待核对切换到 `/workbench/recapture` 后刷新，仍保持“待重新拍照”。
- 空队列不再同时显示队列占位和空状态，只保留一个带上传、查找动作的业务空状态。

Task 2 定向验证：前端 `7` 个测试文件、`38 passed`；TypeScript 类型检查通过。真实浏览器确认四个一级入口、三个内部队列标签、一级页面无返回按钮及队列刷新定位。完整回归仍按计划只在 Task 16 运行。

### Task 3：物理纸张、静态元素和拼版领域模型（已完成）

- `PageSpec.custom(...)` 支持宽 `80—420 mm`、高 `60—594 mm` 和 `0.1 mm` 精度，并按 DPI 生成规范像素尺寸；补充 A4/A5 横向规格。
- `StaticElement` 与识别字段明确分离，使用 `ElementKind` 表达标题、标签、表格线、勾选框、签字线、角色分区和裁切线；静态标题/标签类型不能伪装为 `FieldDefinition`。
- `PrintImposition` 表达承载纸、行列、横纵间距、边距和裁切线，并计算槽位尺寸与单表是否可容纳；拼版不改变单表模板和归一化坐标。
- 发布前检查改为物理毫米规则：5 mm 外边距、29 mm 二维码安全区、12 mm 四角标记区，以及数字格、工号格、勾选框和手写行的最小成品尺寸。
- 草稿可维护静态元素和拼版；克隆发布版时复制两者；发布、弃用或退役版本仍拒绝任何版面或拼版修改。

Task 3 定向验证：领域与应用测试 `19 passed`；相关 Ruff 和 mypy 通过。数据库持久化、API 与前端类型分别属于 Task 5、Task 6，本 Task 没有越界提前实现。完整回归仍按计划只在 Task 16 运行。

### Task 4：字段填写、识别和审核填入规则（已完成）

- 新增 `PaperEntryMode`、`RecognitionMode`、`FillPolicy`，分别表达纸面怎么填、系统怎么产生候选、候选怎样进入最终填写值；现有 `input_type/recognition_engine` 仅作为兼容映射。
- 不自动识别只允许人工填写且不接受可靠度阈值；手写、逐位数字、印刷文字和 OMR 支持仅建议或按阈值预填；二维码支持建议或条件预填。
- 自动计算只能使用计算填入策略、无需纸面填写且必须有计算表达式，不能带 OCR/OMR 或可靠度阈值。
- `worker_name/employee_name` 自动标记为必须人工确认；即使 OCR 可靠度高也不能使用条件自动预填。
- `with_recognition_mode(...)` 在切换识别方式时清除旧阈值和计算式，并为不识别、候选识别、自动计算设置对应默认填入策略。
- 发布前检查新增字段行为、阈值、计算规则和姓名人工确认问题；非法草稿不能进入发布状态。

Task 4 定向验证：领域与应用测试 `23 passed`；相关 Ruff 和 mypy 通过。持久化兼容迁移属于 Task 5，API/前端契约属于 Task 6。完整回归仍按计划只在 Task 16 运行。

### Task 5：新版版面与字段行为持久化（已完成）

- 新增追加式 Alembic `008`，只为 `template_versions` 增加 `static_elements` 和 `print_imposition` JSON；旧迁移 `001—007` 未修改。
- 自定义毫米纸张保留小数精度；静态元素、承载纸/行列/间距/边距/裁切线、三个字段行为枚举、阈值、姓名人工确认和计算式均可完整写入并重载。
- 旧字段 JSON 没有新键时，仓储按旧 `input_type/recognition_engine/minimum_prefill_confidence` 推导新模型；旧模板默认静态元素为空、无拼版，不覆盖历史内容。
- 仓储增加生命周期二次防线：发布、弃用和退役版不能直接替换版面、字段、静态元素、拼版或父版本，也不能直接删除；合法发布和状态退役仍可执行。
- 非 Alembic 自动建库兼容路径也会幂等补齐新列，避免旧本地库因 `create_all` 不会追加列而启动失败。

Task 5 定向验证：领域、应用、仓储和迁移测试 `45 passed`；相关 Ruff 和 mypy 通过。验证了空库直接升级到 `008`、`007` 旧库保留旧模板数据后升级，以及发布版仓储保护。完整回归仍按计划只在 Task 16 运行。

### Task 6：模板 API 与前端类型（已完成）

- 建模板接口保留旧 `page_size: A4/A5`，并新增结构化 `page` 请求，支持 A4/A5 横向和规定范围内的自定义毫米纸张。
- 模板版本响应统一返回规范纸张、静态元素、拼版和完整字段行为；字段请求可发送三个新枚举、阈值、姓名人工确认和计算式，同时保留旧输入/识别键。
- TypeScript 新增严格的 `TemplatePageInput` 判别联合、纸面/识别/填入枚举、静态元素和拼版接口；模板领域契约没有用 `unknown` 或任意字典绕过检查。
- 旧 `createDraft(..., "A4" | "A5", ...)` 保持不变，新增 `createDraftForPage(...)` 发送结构化纸张。
- 发布版字段修改继续返回 `INVALID_LIFECYCLE`；生命周期、字段和纸张错误会映射为包含下一步操作的中文提示。

Task 6 定向验证：Python API/仓储测试 `15 passed`；前端 `3` 个测试文件、`14 passed`；TypeScript、相关 Ruff 和 mypy 通过。完整回归仍按计划只在 Task 16 运行。

### Task 7：安全字段新增与毫米模块编辑器（已完成）

- 新增字段使用独立对话框和全新内部草稿，不读取或修改当前选中字段；字段键在请求前校验唯一，快速重复点击只发一个请求，失败恢复版本与选中状态。
- 字段复制、添加和删除支持结构操作撤销/重做；拖动、缩放、键盘移动和六向对齐支持几何撤销/重做，并继续遵守二维码、实例码、定位标记和打印边界保护区。
- 画布默认使用 1 mm 网格吸附，显示横纵毫米标尺、毫米位置/尺寸、60%—160% 缩放和左/中/右、顶/中/底对齐。
- 左栏只保留模块与图层；右栏按常用设置、填写与识别、校验规则、Excel 导出和高级设置分组，技术键与毫米坐标收进高级设置。
- 数据类型或识别方式切换会清理不适用的数值范围、阈值、计算式和填入策略，并同步兼容的纸面控件、旧识别键和数据类型。
- 发布版在编辑路由仍保持只读，只提供“克隆为新草稿后修改”，并对克隆操作做重复点击保护。

Task 7 定向验证：前端 `7` 个测试文件、`23 passed`；TypeScript 与 Vite 生产构建通过。真实浏览器在真实 API 上确认独立新增对话框、毫米标尺、网格、110% 缩放、水平对齐及撤销恢复；无业务控制台错误，验收草稿和本地服务已清理。完整回归仍按计划只在 Task 16 运行。

### Task 8：三类母版与十个车间模板（已完成）

- 建立计时、等级计件、固定生产明细三类可复用母版，并编码计时工、叉车工、装架组、竹丝装架、干燥组、蒸煮、炭化、锅炉/导热油炉、热压和开片十张明确中文业务表。
- 每张成品模板都带独立结构清单、中文标题、人员/业务/质量/签字角色分区、表格线、勾选框、签字线、数字格和稳定 Excel 导出目标；不再使用“明细第 1 行”类编号占位名称。
- 八张 A5 表配置为 A4 两联，热压和开片使用 A4 横向单表；布局逐张通过 5 mm 边界、双二维码、四角标记和控件最小毫米尺寸预检。
- 安装器在保留四张历史兼容模板的基础上安装十张新模板，共 14 个发布 V1、28 个基础 PNG/PDF 产物；重复安装不新增版本或产物，缺失产物可修复。
- 内容指纹新增静态版面和拼版，并统一 SQLite 整数/浮点毫米表示；真实冲突拒绝覆盖既有发布版，等价内容不会误报冲突。

Task 8 定向验证：模板结构与 seed 安装器 `12 passed`；相关 Ruff 和 mypy 通过。完整回归仍按计划只在 Task 16 运行。

### Task 9：真实中文业务表打印渲染（已完成）

- 单表 300 DPI PNG/PDF 现在按静态元素和纸面填写方式绘制中文标题、角色分区、表格外框、文本框、逐位数字格、勾选框和签字线，不再只打印英文技术键。
- 四个 12 mm 定向标记继续保留；模板码和实例码在各自 29 mm 安全区内使用带静区的 18 mm 符号，避开右上定位标记并可独立解码。
- 有两联配置的 A5 模板额外生成 `PRINT_IMPOSED_PDF`；每个槽位独立渲染，实例序号逐张递增，并按配置绘制裁切线。四张旧模板继续生成原 PNG/PDF。
- 发布 API 在改变版本状态前验证中文字体；缺失时返回 `CHINESE_FONT_UNAVAILABLE`，版本保持 `READY_TO_PUBLISH` 且不产生半成品。可通过 `FORM_DEMO_CJK_FONT_PATH` 指定字体，默认发现微软雅黑、思源/Noto 中文字体。
- seed 安装器把拼版 PDF 纳入完整性和缺失修复判断；当前 14 个 V1 共生成 36 个基础打印产物。

Task 9 定向验证：渲染器、模板 API 与 seed 安装器 `25 passed`；相关 Ruff 和 mypy 通过。另已实际查看计时工 A5 PNG，修正过大字段字号、网格穿字和重复勾选框。完整回归仍按计划只在 Task 16 运行。

### Task 10：二维码导入与精确版本路由（已完成）

- OpenCV 现在读取一张图片中的全部二维码；整图检测失败时只回退扫描约定的页头二维码保留区，不按版面文字、文件名或相似度猜测模板。
- 十张真实模板的导出 PNG 全部解析到各自准确的模板键和发布版本；蒸煮表长载荷码在页头区域回退路径中同样可稳定读取。
- A4 两联裁开后的左右 A5 表均同时读取模板码和自己的实例码，分类来源记为 `SHEET_QR`，实例序号分别保留；实例码单独出现时仍不能推断模板，必须进入人工分类。
- 同图出现两个不同模板身份或两个不同实例身份时，表单进入 `NEEDS_CLASSIFICATION`，不修改原模板引用，并追加 `CLASSIFICATION_CONFLICT` 审计，保存全部冲突二维码。
- 损坏、篡改、未发布或未知模板码继续进入人工分类；现有单码导入、透视校正和字段裁切链路保持兼容。

Task 10 定向验证：领域、识别集成、导入 API `32 passed`，既有二维码/验收兼容 `6 passed`，合计 `38 passed`；相关 Ruff 和 mypy 通过。完整回归仍按计划只在 Task 16 运行。

### Task 11：统一图片与结构化数据审核工作台（已完成）

- 待确认类型、待核对和待重新拍照三队列继续使用同一工作台外壳；待确认类型队列现在也在左侧展示图片证据、右侧完成精确模板选择和原因填写。
- 工作台字段接口补充纸面填写方式、识别方式、审核填入方式和必须人工确认标记；字段详情同时展示原图裁片、行为摘要、填报规则和错误。
- 不自动识别字段不会显示遗留候选或可靠度；姓名字段即使存在旧候选也必须对照原图裁片人工确认，未确认时优先进入问题导航。
- 既有原图/校正图切换、缩放、旋转、复位、字段框与字段行双向联动、首个问题定位、固定底部操作和不适用按钮隐藏保持不变。

Task 11 定向验证：后端工作台 API `7 passed`，前端 8 个相关测试文件 `35 passed`；TypeScript、Vite 生产构建、相关 Ruff/mypy 和真实 Edge 正确路由冒烟通过。完整回归仍按计划只在 Task 16 运行。

### Task 12：工号、姓名与角色化复核（已完成）

- `worker_number` 和兼容的 `employee_id` 按在职员工主数据比对；命中时显示“工号已匹配”，未知或停用编号显示“工号未在员工库中匹配，必须人工处理”，后端以专用规则错误阻止提交。
- 姓名字段不会因为候选可靠度高而自动放行；工作人员必须点击“确认姓名与原图一致”，客户端提交显式人工确认字段，后端再次验证后才允许写入记录。
- 审核电子表格按工人填写、质量填写、主管填写、签字确认四组排序和展示；角色分组随工作台 API 返回，旧模板字段安全回退到工人填写。
- 人工确认与更正继续追加不可变记录版本和审计事件；识别候选不覆盖，候选/审计时间、操作人、原因和前后值沿用现有追溯链。

Task 12 定向验证：后端审核工作台/工作流 `16 passed`，主数据 `8 passed`，前端 6 个相关测试文件 `29 passed`；TypeScript、Vite 生产构建、相关 Ruff/mypy 通过。完整回归仍按计划只在 Task 16 运行。

### Task 13：重构导出数据前端流程（已完成）

- 导出流程由四步整理为“选择数据 → 检查数据 → 生成并下载”三步，步骤编号不再重复；导出记录作为独立区域保留，不再冒充流程步骤。
- 检查结果只在工作人员主动检查后显示；筛选或导出类型变化会中止旧检查、清除旧结果并持续提示重新检查，禁止使用失效快照生成 Excel。
- 生成卡片显示任务进度；生成失败时在当前步骤展示原因和“重新生成 Excel”，成功后可立即下载，同时刷新独立导出记录。
- 主流程隐藏模板 ID、任务/批次 ID、完整哈希和超长文件名；这些技术信息仅在工作人员主动展开追溯详情时显示，业务文案统一使用“Excel”。

Task 13 定向验证：导出 API/页面组件 `17 passed`；TypeScript 类型检查和 Vite 生产构建通过。完整回归仍按计划只在 Task 16 运行。

### Task 14：完成十个模板的稳定 Excel 导出映射（已完成）

- 十个真实工资表各追加不可变 V2，V1 的版本 ID、字段、打印和旧导出目标均不修改；种子安装器可在已有 V1 数据库中只追加 V2。完整种子现为 14 个模板键、24 个发布版本和 64 个打印产物。
- V2 使用“工资主记录”和“业务明细”两张稳定工作表；主记录覆盖人员、考评、事实说明和三方签字，业务明细按模板字段顺序输出中文业务列。
- 热压和开片固定生产明细以 `export_batch_id + form_id + record_version` 同时关联主记录和明细行；预览映射按模板字段顺序稳定生成，不再按字段键字母顺序打乱列。
- 整数、金额/小数、布尔和文本保持 Excel 原生单元格类型；四个历史通用 V1 继续使用原单工作表和字段键，不回归。

Task 14 定向验证：模板/种子/Excel 集成测试 `41 passed`；相关 Ruff、格式检查和 mypy 通过。完整回归仍按计划只在 Task 16 运行。

### Task 15：统一工作人员中文文案（已完成）

- 任务状态统一映射为工作人员可理解的短文案、说明和下一步动作，组件不再把后端状态码当作主文案。
- 审核、字段、基础数据、Excel 和模板版本操作的终端用语已统一；Lease 冲突与失效使用可执行的中文提示。
- 模板、任务、状态和步骤技术信息保留在追溯详情，日常主界面仅展示业务语义。

Task 15 定向验证：前端 `14` 个测试文件、`74 passed`；TypeScript 类型检查和 Vite 生产构建通过。完整回归仍按计划只在 Task 16 运行。

### Task 16：自动化回归与代表模板闭环（自动/模拟已完成，实体待复验）

- 计时工 A5、蒸煮 A4 两拼和开片 A4 横向三个代表模板均完成“发布版→PNG/PDF→模拟填写/拍照→二维码分类→透视校正→字段裁片→人工复核→Excel”。
- PNG/PDF 物理尺寸、高分辨率阴影照片二维码、字段中心 10% 容差、数字格有效书写区、蒸煮两拼独立序号 41/42、工资主记录/业务明细和六类追溯事件均通过。
- 验收发现并修复打印 5 mm 边距/12 mm 方向标记与校正器目标不一致的字段偏移，并为 OpenCV 增加不改变身份安全边界的多尺度二维码回退。

Task 16 完整验证：Python `309 passed`；Ruff 通过；mypy `129 source files` 无问题；前端 `31` 个测试文件、`146 passed`；TypeScript 和 Vite 生产构建（`78 modules`）通过。详细验收与实体设备边界见 [PAPER_TEMPLATE_ACCEPTANCE.md](acceptance/PAPER_TEMPLATE_ACCEPTANCE.md)。

实体复验准备：新增可重复生成三个代表模板 PNG/PDF、A5 两拼 PDF、SHA-256 manifest 和现场记录清单的 `app.tools.build_paper_acceptance_pack_ds`。生成器定向测试 `3 passed`，Ruff 通过，mypy 在新工具加入后为 `130 source files` 无问题。本地验证包已生成在 `.runtime/paper-acceptance-pack-ready`。

当前无剩余代码开发项；Task 16 仅剩需要真实打印机、手机、卡尺和纸质样表的实体复验。

## 已完成的前端能力

| 模块 | 当前可用能力 |
|---|---|
| 应用外壳与路由 | 固定一级导航；审核、模板、基础数据、导出均有可复制 URL；刷新、前进和后退保持模块位置；未知地址显示应用内 404。 |
| 审核工作台 | 待确认类型、待核对、待重新拍照共用工作台骨架；桌面双栏、可拖动分隔线、窄屏图片/电子表格切换；队列、表单上下文和底部动作统一。 |
| 图片证据 | 原图/校正图切换、25%–400% 缩放、滚轮、拖动、90° 旋转、复位、字段框显示隐藏和字段裁片。 |
| 字段审核 | 图片框与字段行双向联动；首个问题字段定位；候选、规则、主数据选项和错误说明；草稿、首次确认、更正、退回、作废保持原 API 语义。 |
| 分类与任务进度 | 二维码失败时必须选择精确已发布版本并填写原因；分类后原地轮询识别任务，显示中文步骤、进度、失败原因和重试方向。 |
| 模板中心 | 顶部搜索/筛选/创建工具栏；精确版本路由；发布版本只读；调优克隆草稿；预览支持缩放、适合页面/宽度、搜索和双向选择；模板包导入仍明确为未开放。 |
| 基础数据 | 员工、工单、产品、工序按 URL 正确定位；业务字段表单替代直接 JSON；未知扩展属性保留在“更多信息”；版本冲突、停用/恢复和审计语义保留。 |
| 导出数据 | “选择数据 → 检查数据 → 生成并下载”三步流程；筛选变化强制重新检查；任务进度、失败重试和即时下载完整；导出记录独立；技术信息只在追溯详情显示；重导保留旧文件和替代关系。 |
| 通用交互 | 统一问题提示、危险操作确认框、追溯详情和状态徽标；主要按钮使用明确的“动作 + 对象”文案，键盘和焦点行为有测试覆盖。 |

## 验证结果

### 自动化验证

- Python：`309 passed`。测试使用代码默认身份 `local-operator/OPERATOR` 覆盖仓库本地 `.env` 的管理员试用配置，并使用仓库内临时目录避开系统临时目录权限限制。
- Ruff：通过。
- mypy：`129 source files` 无问题。
- 前端：`31 passed` 测试文件、`146 passed` 测试；TypeScript 类型检查通过。
- Web 生产构建：通过，Vite 转换 `78 modules`。

### 真实浏览器验收

- 在真实 API、真实 Vite Web 和现有表单数据上核对了统一外壳、审核队列、20 个字段框、字段表格、原图/校正图、缩放、旋转、复位、字段裁片及首个问题字段。
- 核对了模板创建弹窗、产品 URL 直达和产品业务表单、导出四步检查结果及重拍队列空状态。
- 在 `390 × 844` 视口核对了图片/电子表格切换，字段选择状态保持正确。
- 浏览器验收发现后端排除原因仍可能返回英文；已在 `e04b617` 增加按原因码映射的中文文案和回归测试，真实页面复验后不再显示对应英文原因。
- 调试复测发现历史正式记录可能按运行时字段 ID 保存，导致刷新后低置信度人工确认值再次显示“待确认”，并可能让模板导出列取不到值；`ff03a39` 已统一工作台正式值覆盖、人工确认来源和导出字段键映射，并增加前后端回归测试。
- 为避免改变共享演示数据，浏览器验收未提交审核、分类、停用或导出等写操作；这些写入分支由组件/API 自动化测试覆盖。

## 仍未完成或受阻的能力

1. **重新拍照的永久追溯仍受后端阻塞**：现有 `POST /api/v1/imports` 只会创建新表单，尚无生产 API 将新照片绑定为 `RECAPTURE_REQUIRED` 原表单的 replacement evidence。前端只诚实提示限制，不使用内存或 `localStorage` 冒充永久关系。
2. **真实现场识别尚未量化**：仍需要 40–60 张来自不同打印机、手机、光线、折痕、倾斜和二维码污损条件的企业样表，才能给出现场准确率结论。
3. **生产任务消费未完全独立化**：`FORM_IMPORT`、`FORM_RECOGNITION`、`XLSX_EXPORT` 尚未全部由独立生产 Worker/Handler 消费。
4. **模板包和纸张实例未实现**：安全 ZIP 导入/导出、manifest/hash 校验、打印批次和唯一 `sheet_instance_id` 仍需独立任务。
5. **中文 OCR 与桌面设备接入未完成**：文本字段仍以人工核对为主；Tauri、相机、扫描仪、Windows 安装包仍是后续工程。
6. **企业现场实体复验尚未执行**：最终开发计划和三个代表模板的数字模拟纸面闭环已完成，但本环境没有实体打印机、手机和卡尺。投产前仍必须以 100% 打印并完成 40—60 张跨打印机、手机、光线、折痕和污损的样表统计，不得用模拟 3/3 结果代替。

## 继续协作

协作者应先读取 [NEXT_TASK.md](NEXT_TASK.md)、[当前最终实施计划](superpowers/plans/2026-07-18-industrial-paper-templates-implementation.md) 和 [DECISIONS.md](DECISIONS.md)。旧的[前端重建计划](superpowers/plans/2026-07-17-frontend-rebuild-implementation.md)只保留为历史记录，不需要重新执行。拉取命令：

```powershell
git fetch origin
git switch modular-architecture
git pull --ff-only origin modular-architecture
git status --short
```

预期工作树干净。新增工作按最终计划的 Task 1—16 顺序实施。已验证且本轮未修改的简单功能不得重复研究或重复人工验收；每个任务只运行相关测试，完整自动回归只在 Task 16 运行一次，实物闭环只覆盖三个代表模板。
