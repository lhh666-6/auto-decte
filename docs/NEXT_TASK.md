# 后续任务

最后更新：2026-07-19

状态：岗位配置双版本打印、识别和持久化闭环已完成；当前只执行下面一个纸表组件任务。

目标分支：`modular-architecture`

## 当前唯一任务：受控明细表网格组件

规格来源：[工业纸质工资表系统：低 Token 实施与统一 UI 规范](superpowers/specs/2026-07-19-industrial-payroll-low-token-ui-v2.md) 第 3、4 章。

执行计划：[Controlled Table Grid Primitive Plan](superpowers/plans/2026-07-20-controlled-table-grid.md)。

目标：让固定明细表拥有真实的行数、列数和受控列宽，打印时绘制内部线条；不再把 TABLE_GRID 当成空外框。

只读取和修改：

- `app/domain/templates_ds.py`
- `app/adapters/templates/print_renderer_ds.py`
- `app/adapters/database/template_repository_ds.py`
- 相关领域、renderer、模板仓储测试

只运行领域、renderer、模板仓储测试、Ruff 与相关 mypy，不重复运行 API、前端、审核工作台、导出或完整套件。

## 当前执行状态

[工业纸质工资表闭环与前端交互重构最终计划](superpowers/plans/2026-07-18-industrial-paper-templates-implementation.md)的所有代码开发、Task 16 完整自动回归和数字模拟闭环已执行。当前唯一未完验收项是计时工、蒸煮两拼和开片横向三个代表模板的实体 100% 打印、手机拍摄、尺寸测量与同一闭环复验。

Task 1 已交付：

- `APP_AUTH_MODE=local_full_access` 默认放行本地完整业务流程，`authenticated` 模式保留角色权限矩阵；
- 本地写操作仍使用 `local-operator` 写入审计；
- 删除页头伪用户和当前身份入口；
- 统一中文错误边界，页面不展示权限代码或内部权限名；
- 定向 Python 13 项、前端 8 项、TypeScript、相关 Ruff/mypy 和浏览器冒烟检查通过。

Task 2 已交付：

- 品牌统一为不可点击的“工业工资表系统”，顶部一级导航固定为审核工作台、模板中心、基础数据、导出数据；
- 审核页删除重复页头、左侧模块入口和“可导出”队列，三类审核任务改为工作台内部标签；
- 一级模块删除“返回审核工作台”，三个审核队列继续使用独立 URL 并可在刷新后保留位置；
- 空队列只显示一个业务空状态；定向前端 38 项、TypeScript 和真实浏览器路由检查通过。

Task 3 已交付：

- 自定义物理纸张支持规定毫米范围、0.1 mm 精度和规范像素换算，并补充 A4/A5 横向规格；
- 静态标题、标签、线框、表格、勾选框、签字线、角色分区和裁切线使用非识别 `StaticElement`，不能冒充业务字段；
- 拼版模型记录承载纸、行列、间距、边距和裁切线，并检查每个槽位是否容纳单表；
- 发布前按毫米检查 5 mm 外边距、二维码/定位标记保护区和各类填写控件最小尺寸；发布版不可修改；
- 定向领域/应用测试 19 项、相关 Ruff 和 mypy 通过；持久化和 API 仍按顺序留给 Task 5—6。

Task 4 已交付：

- 纸面填写方式、系统识别方式和审核填入方式使用三个独立枚举，旧识别字段保留兼容映射；
- 不识别、OCR/OMR、二维码和自动计算分别有明确合法填入策略与阈值规则；
- 姓名强制人工确认，识别方式切换清除隐藏的旧阈值和计算式；
- 发布前检查阻止行为、阈值、纸面控件、计算规则和姓名规则的非法组合；
- 定向领域/应用测试 23 项、相关 Ruff 和 mypy 通过；下一步必须完成追加式迁移和旧模板兼容读取。

Task 5 已交付：

- 追加迁移 `008` 为模板版本增加静态元素与拼版存储，旧 `001—007` 保持不变；
- 自定义毫米纸张、静态版面、拼版和完整字段行为可写入、重载，0.1 mm 尺寸不被截断；
- `007` 旧库和旧字段 JSON 可无损升级读取，自动建库兼容路径幂等补列；
- 仓储拒绝绕过领域层替换或删除发布版，同时允许合法状态退役；
- 定向领域、应用、仓储、空库/旧库迁移测试 45 项、相关 Ruff 和 mypy 通过。

Task 6 已交付：

- 模板 API 返回纸张、静态元素、拼版和完整字段行为，结构化请求支持横向和自定义毫米纸张；
- 旧 `page_size: A4/A5` 和旧 TypeScript `createDraft` 调用保持兼容；
- 前端使用判别联合与明确接口表达新版模板，不使用 `unknown` 或任意字典逃避类型检查；
- 发布版修改仍被拒绝，生命周期、字段、纸张错误映射为工作人员可执行的中文提示；
- 定向 Python 15 项、前端 14 项、TypeScript、相关 Ruff 和 mypy 通过。

Task 7 已交付：

- 新增字段与编辑字段状态隔离，使用独立对话框；唯一键、重复点击锁、失败回滚和成功选中均有测试；
- 添加、复制、删除具备结构撤销/重做，拖动、缩放、键盘移动和对齐具备几何撤销/重做；
- 画布提供 1 mm 吸附、毫米标尺/尺寸、60%—160% 缩放和六向对齐，继续执行保护区约束；
- 左栏只保留模块与图层，右栏业务属性分组，技术键和毫米坐标进入高级设置；
- 数据类型/识别方式变化会清除不适用配置并同步兼容控件；发布版只读，修改必须克隆草稿；
- 定向前端 7 个文件 23 项、TypeScript、Vite 生产构建和真实浏览器冒烟通过。

Task 8 已交付：

- 计时、等级计件、固定生产明细三类母版承载十张明确的中文车间工资表，不以母版本身冒充成品模板；
- 每张模板具有结构清单、角色分区、表格线、数字格、勾选框、签字线和稳定导出目标，禁止编号占位名称；
- 八张 A5 表使用 A4 两联，热压和开片使用 A4 横向单表，十张布局逐一通过物理保护区预检；
- 四张旧模板继续保留，新库幂等安装共 14 个发布 V1 和 28 个基础打印产物；等价内容可重复安装，冲突内容不会覆盖历史发布版；
- 定向结构/安装器 12 项、相关 Ruff 和 mypy 通过。

Task 9 已交付：

- 300 DPI 单表 PNG/PDF 渲染中文标题、角色区、表格、文本框、数字格、勾选框和签字线，旧模板保持兼容；
- A5 两联额外生成拼版 PDF 和裁切线，每个槽位独立生成模板码与递增实例码；
- 29 mm 二维码安全区内的 18 mm 带静区符号避开四角定位标记，自动测试逐半张解码成功；
- 中文字体在发布状态改变前验证，缺失时不发布、不生成半成品，可通过 `FORM_DEMO_CJK_FONT_PATH` 指定；
- seed 产物完整性包含拼版 PDF，14 个 V1 共 36 个基础产物；定向测试 25 项、相关 Ruff/mypy 和实际 PNG 目视检查通过。

Task 10 已交付：

- 多码读取同时识别模板码和实例码，整图失败只扫描固定页头保留区，不依据内容猜模板；
- 十张真实模板 PNG 全部精确路由到对应发布 V1，两联左右裁开后分别保留自己的实例序号；
- 有实例码时分类来源优先记为 `SHEET_QR`，但实例码不携带模板键，缺少有效模板码时仍进入人工分类；
- 多个不同模板或实例身份进入人工处理，保持原模板引用并写 `CLASSIFICATION_CONFLICT` 审计；
- 损坏、篡改、未发布码及旧单码链路保持兼容；相关定向与兼容测试合计 38 项、Ruff/mypy 通过。

Task 11 已交付：

- 待确认类型、待核对和待重新拍照三队列使用统一双栏工作台骨架，分类队列不再脱离图片证据；
- 原图/校正图、缩放、旋转、复位、字段框与字段行双向联动、首个问题定位和固定底部操作继续保持；
- 工作台 API 返回纸面填写、识别、填入和必须人工确认行为，当前字段详情明确显示裁片、行为与规则；
- 不自动识别字段不显示历史候选或可靠度，姓名字段始终要求对照原图裁片人工确认；
- 后端 API 7 项、前端工作台 35 项、TypeScript、生产构建、相关 Ruff/mypy 和真实 Edge 路由冒烟通过。

Task 12 已交付：

- 工号按员工主数据有效状态实时比对，命中显示“工号已匹配”，未知或停用工号由前后端共同阻止自动放行；
- 姓名必须通过“确认姓名与原图一致”显式确认，提交契约携带已人工确认字段，后端对缺少声明的姓名拒绝确认；
- 字段按工人填写、质量填写、主管填写和签字确认四组展示，分组由工作台稳定响应提供；
- 人工确认继续追加记录版本和审核事件，原识别候选不覆盖，候选时间、审核时间、操作人和原因均保留；
- 后端审核/主数据 24 项、前端 29 项、TypeScript、生产构建、相关 Ruff/mypy 通过。

Task 13 已交付：

- 导出界面统一为选择、检查、生成并下载三步，修复重复编号，导出记录移出主流程并保持独立刷新和下载；
- 检查结果按操作渐进显示，筛选条件或导出类型变化会中止并废弃旧检查，持续提示重新检查；
- 生成任务显示进度，失败原因和重新生成入口留在当前步骤，成功后可立即下载并刷新记录；
- 主流程隐藏技术 ID、完整哈希和超长文件名，工作人员文案统一使用“Excel”，技术信息仅在主动展开的追溯详情内显示；
- 导出 API/页面组件 17 项、TypeScript 类型检查和 Vite 生产构建通过。

Task 14 已交付：

- 十个真实工资表保留不可变 V1，并分别新增 V2 稳定 Excel 映射；已有数据库只追加 V2，不重写历史 V1；完整种子现为 14 个模板键、24 个发布版本和 64 个打印产物；
- V2 统一输出“工资主记录”和“业务明细”，主记录按人员、考评、事实说明和签字的固定业务顺序排列，明细列保持各模板字段顺序；
- 热压和开片固定生产表按相同批次 ID、表单 ID、记录版本关联主记录和明细行；数值、布尔和文本单元格保持正确 Excel 类型；
- 四个历史通用 V1 继续按原工作表和字段键导出，原映射、批次与文件语义不变；
- Task 14 定向 Python 测试 41 项、Ruff、格式检查和相关 mypy 通过。

Task 15 已交付：

- 建立任务状态的中文短文案、解释和下一步动作，页面不再直接显示后端状态码；
- 审核操作统一为“开始审核 / 你正在审核”，字段术语统一为“识别可靠度 / 最终填写值 / 基础数据”；
- 导出操作使用“生成 Excel”，发布版调整入口使用“基于此版本创建新草稿”；
- 模板 ID、任务 ID、状态码和步骤码仅在主动展开的追溯详情中显示；
- Task 15 定向前端 14 个测试文件、74 项全部通过；TypeScript 类型检查和 Vite 生产构建通过。

Task 16 已交付的自动化与模拟部分：

- 新增三个代表模板的端到端验收：已发布 V2 → 300 DPI PNG/PDF → 模拟填写、轻微透视/旋转/阴影 → 照片导入 → 精确二维码分类 → 透视校正 → 字段裁片 → 人工复核 → Excel；
- 计时工 A5、蒸煮 A4 两拼和开片 A4 横向 3/3 通过；蒸煮两拼左右小表独立识别序号 41/42；
- 修复打印方向标记与透视校正中心不一致导致的字段偏移，并增加高分辨率阴影照片二维码多尺度回退；
- 完整验证为 Python `309 passed`、Ruff 通过、mypy `129 source files` 通过、前端 `31` 个文件/`146 passed`、TypeScript 和 Vite `78 modules` 生产构建通过；
- 详细数据见 [PAPER_TEMPLATE_ACCEPTANCE.md](acceptance/PAPER_TEMPLATE_ACCEPTANCE.md)。本轮未使用实体打印机、手机和卡尺，现场复验仍是投产前置条件。
- 已提供 `app.tools.build_paper_acceptance_pack_ds` 生成器，可一次产出三个代表模板的实体打印包、SHA-256 manifest 和现场记录清单；定向测试 `3 passed`、Ruff 和 mypy `130 source files` 通过。

当前剩余验收：用真实设备完成三个代表模板的实体闭环，并把照片、卡尺数据、打印设置和结果追加到 [PAPER_TEMPLATE_ACCEPTANCE.md](acceptance/PAPER_TEMPLATE_ACCEPTANCE.md)。在获得这些外部证据前，不得将 Task 16 或整个计划标记为完全完成。

本阶段明确交付：

- 本地无登录完整权限和工作人员中文错误；
- 去重后的导航、审核工作台、模板中心、基础数据和导出数据；
- 纸面填写方式、识别方式和审核填入方式的独立规则；
- 任意毫米纸张、横竖方向、毫米网格、小模块和多联拼版；
- 计时、简单计件、复杂生产明细 3 类母版；
- 10 个真实车间工资表模板；
- 真实中文 PDF/PNG、独立二维码、打印后精确版本识别；
- 工号员工库比对、姓名人工确认和稳定 Excel 导出；
- 3 个代表模板的实物闭环。

### 节省 Token 和验收成本的强制规则

- 不扫描整个仓库，只读最终计划和当前 Task 列出的相关文件。
- 已有测试记录且本轮未修改的简单功能直接视为基线，不重新研究、不重复实现、不重复人工验收。
- 每个 Task 只运行相关测试；共享领域、迁移、公共 API、二维码或导出结构变化时才扩大到受影响的集成测试。
- 文案、按钮、空状态、导航高亮等简单修改只做组件测试和一次浏览器冒烟检查，不跑纸张闭环。
- 十个模板全部做结构化自动测试；实物闭环只验收计时工、蒸煮或炭化两拼、开片横向复杂表。
- 完整 Python 回归、Ruff、mypy、前端全测和生产构建只在 Task 16 统一运行一次。
- 同一个通过结果只记录一次；没有相关代码变化或失败证据时，不重复截图、日志和测试统计。

## 本阶段外仍需保留的已知阻塞

1. **重新拍照永久证据关联**：现有导入接口仍不能把新照片永久绑定到 `RECAPTURE_REQUIRED` 原表单；不得用浏览器状态伪造关联。
2. **大规模现场准确率量化**：40—60 张真实企业样表的跨打印机、手机、光线、折痕和污损统计属于后续现场验证，不在本轮三个代表模板验收内。
3. **生产 Worker 与桌面设备交付**：独立 Worker、Tauri、相机/扫描仪、Windows 安装包和升级机制需要后续单独授权。

## 已完成，不应重复开发

- 统一双栏审核工作台、图片工具、字段联动、首个问题定位和窄屏切换。
- 二维码失败后的精确版本分类和原地识别任务进度。
- 统一路由、应用外壳、中文业务文案、问题提示和危险操作确认。
- 模板中心基础路由与预览工具、基础数据业务表单、现有导出基础流程。
- 旧前端计划记录的 Python、Ruff、mypy、前端测试、类型检查、生产构建和浏览器验收；这些结果只作为基线，不冒充新计划的最终验收。

## 开始下一任务前

当前范围已经确认，不需要再次询问是否实施纸面模板。协作者只需读取：

- [CURRENT_STATUS.md](CURRENT_STATUS.md)
- [工业纸质工资表闭环与前端交互重构最终计划](superpowers/plans/2026-07-18-industrial-paper-templates-implementation.md)
- [DECISIONS.md](DECISIONS.md)

不得改变审核 Lease、记录版本、发布模板只读、主数据乐观锁、导出预览失效与不可变批次等既有业务语义。旧前端计划只作为历史记录，不需要读取或重复执行。
