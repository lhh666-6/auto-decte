# 受控字段 API 与设计器贯通计划

## 目标

保证后端领域中已生效的填写约束经过 API 读取、前端编辑和保存后不会丢失。

## 步骤

1. 模板字段请求和响应加入数字位数、选项组、固定选项、最大选择数、系统带入来源、条件必填和签字角色。
2. TypeScript `TemplateField` 与新字段草稿提供稳定默认值。
3. 字段属性面板只在相应纸面组件下显示业务化配置项，并在切换组件时清除不适用的旧值。

## 验证

- 模板 API 单文件定向测试；
- 字段属性面板组件测试；
- 前端 TypeScript；
- 相关 Ruff 与 mypy。
